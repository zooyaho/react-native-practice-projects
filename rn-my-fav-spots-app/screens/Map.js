import { StyleSheet, Text, View } from "react-native";

/**
 * 지도를 전체화면으로 표시하는 화면 컴포넌트
 * - 지도상에서 장소를 선택하는 등의 기능 제공
 */
const Map = () => {
  return (
    <View>
      <Text>Map</Text>
    </View>
  );
};

export default Map;
